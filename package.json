{
  "name": "aegis-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix aegis-web run dev",
    "build": "npm --prefix aegis-web run build",
    "start": "npm --prefix aegis-web run start",
    "lint": "npm --prefix aegis-web run lint"
  }
}
